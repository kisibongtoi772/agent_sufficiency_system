# Agent Sufficiency System

Research prototype for measuring and reducing the amount of context an agent needs to complete a task.

The project keeps orchestration separate from context policy so retrieval, memory, and tool-loading strategies can be benchmarked independently.
